# ============================================================================
# Agentic AI Social Media Management System v2.0
# Python 3.11+ Backend Project - Requirements
# ============================================================================

# ============================================================================
# WEB FRAMEWORKS & SERVER
# ============================================================================
fastapi==0.128.0             # Modern, fast web framework for building APIs (updated)
flask==3.0.0                 # Lightweight WSGI web framework
uvicorn[standard]==0.40.0    # ASGI server implementation (updated to match installed)
pydantic==2.12.5             # Data validation for FastAPI (updated)
pydantic-settings==2.12.0    # Settings management for Pydantic v2 (updated)
python-multipart==0.0.6      # Multipart form data parser for FastAPI
starlette==0.50.0            # ASGI framework (FastAPI dependency - updated)

# ============================================================================
# DATABASES
# ============================================================================
psycopg2-binary==2.9.9       # PostgreSQL adapter for Python
pymongo==4.6.1               # MongoDB driver for Python
redis==7.1.0                 # Redis client library (updated)
sqlalchemy==2.0.45           # SQL toolkit and ORM (updated)
alembic==1.18.0              # Database migration tool
motor==3.3.2                 # Async MongoDB driver (works with pymongo)
hiredis==2.2.3               # Fast Redis client (C extension)

# ============================================================================
# AI/ML - Core Libraries
# ============================================================================
openai==2.14.0               # OpenAI API client (GPT-4, GPT-5 support - updated)
transformers==4.36.2         # Hugging Face Transformers library
torch==2.2.0                 # PyTorch deep learning framework
torchvision==0.17.0          # PyTorch vision utilities
torchaudio==2.2.0            # PyTorch audio utilities
accelerate==0.25.0           # Hugging Face Accelerate (optimizes training)
openai-whisper==20231117     # OpenAI Whisper speech recognition model
langchain==0.1.0             # LangChain framework for LLM applications
langchain-openai==0.0.2      # LangChain OpenAI integration
langchain-community==0.0.10  # LangChain community integrations
sentence-transformers==2.2.2 # Sentence embeddings using transformers

# ============================================================================
# AI/ML - Dependencies
# ============================================================================
numpy==1.24.3                # Numerical computing (required by ML libraries)
scipy==1.11.4                # Scientific computing library
scikit-learn==1.3.2          # Machine learning library (core dependency)
tokenizers==0.15.0           # Fast tokenizers for transformers
safetensors==0.4.2           # Safe serialization format for tensors
protobuf==4.25.1             # Protocol buffers (used by ML libraries)

# ============================================================================
# SOCIAL MEDIA APIs & SCRAPING
# ============================================================================
tweepy==4.14.0               # Twitter API v2 client library
yt-dlp==2023.12.30           # YouTube and video platform downloader
instaloader==4.10.3          # Instagram scraper and downloader
tiktok-downloader==0.2.1     # TikTok video downloader

# ============================================================================
# BROWSER AUTOMATION - CRITICAL
# ============================================================================
playwright==1.40.0           # Modern browser automation framework
playwright-stealth==1.0.6    # Anti-detection plugins for Playwright
webdriver-manager==4.0.1     # Automated webdriver management
selenium==4.16.0             # Alternative browser automation (legacy support)
beautifulsoup4==4.12.2       # HTML/XML parser for web scraping
lxml==5.1.0                  # Fast XML/HTML parser (for BeautifulSoup)
html5lib==1.1                # HTML5 parser

# ============================================================================
# MEDIA PROCESSING
# ============================================================================
opencv-python==4.8.1.78      # OpenCV computer vision library
moviepy==1.0.3               # Video editing and processing
pillow==10.1.0               # Python Imaging Library (PIL fork)
ffmpeg-python==0.2.0         # FFmpeg Python wrapper
face-recognition==1.3.0      # Face recognition library
dlib==19.24.2                # Machine learning library (dependency for face-recognition)
imageio==2.33.1              # Image I/O library (for MoviePy)
imageio-ffmpeg==0.4.9        # FFmpeg plugin for imageio

# ============================================================================
# AUDIO PROCESSING
# ============================================================================
pydub==0.25.1                # Audio manipulation library
librosa==0.10.1              # Music and audio analysis library
speechrecognition==3.10.0    # Speech recognition library
soundfile==0.12.1            # Audio file I/O (for librosa)
resampy==0.4.2               # Efficient audio resampling
# Note: scikit-learn is already included in AI/ML dependencies section above

# ============================================================================
# DATA PROCESSING & ANALYSIS
# ============================================================================
pandas==2.1.4                # Data manipulation and analysis
# Note: numpy and scikit-learn are already included in AI/ML dependencies section above

# ============================================================================
# TASK QUEUE & BACKGROUND JOBS
# ============================================================================
celery[redis]==5.3.4         # Distributed task queue (with Redis support)
flower==2.0.1                # Celery monitoring tool
kombu==5.3.4                 # Messaging library (Celery dependency)
billiard==4.2.0              # Multiprocessing pool (Celery dependency)
vine==5.1.0                  # Promises and promises-like objects (Celery dependency)

# ============================================================================
# SECURITY & AUTHENTICATION
# ============================================================================
python-jose[cryptography]==3.3.0  # JWT implementation (with crypto backend)
passlib[bcrypt]==1.7.4       # Password hashing library (with bcrypt support)
bcrypt==5.0.0                # Bcrypt password hashing (updated)
cryptography==46.0.3         # Cryptographic recipes and primitives (updated)
email-validator==2.3.0       # Email validation for Pydantic
dnspython==2.8.0             # DNS toolkit (required by email-validator)
# Note: python-multipart is already included in WEB FRAMEWORKS section

# ============================================================================
# HTTP & API CLIENT
# ============================================================================
httpx==0.28.1                # Modern HTTP client (async/sync - updated)
requests==2.31.0             # HTTP library for humans
aiohttp==3.9.1               # Async HTTP client/server framework
websockets==12.0             # WebSocket implementation

# ============================================================================
# UTILITIES & HELPERS
# ============================================================================
python-dotenv==1.2.1         # Environment variable management (updated)
click==8.3.1                 # Command-line interface creation (updated)
python-dateutil==2.8.2       # Date/time utilities
pytz==2023.3                 # Timezone definitions
tenacity==8.2.3              # Retry library for robustness
tqdm==4.67.1                 # Progress bars (updated)
colorama==0.4.6              # Cross-platform colored terminal text (updated)
loguru==0.7.2                # Better logging

# ============================================================================
# VALIDATION & SERIALIZATION
# ============================================================================
marshmallow==3.20.1          # Object serialization/deserialization
jsonschema==4.20.0           # JSON schema validation
pyyaml==6.0.1                # YAML parser
toml==0.10.2                 # TOML parser

# ============================================================================
# CORE DEPENDENCIES (Auto-installed dependencies - listed for reference)
# ============================================================================
anyio==4.12.1                # Async I/O library (FastAPI dependency)
certifi==2026.1.4            # SSL certificate bundle
h11==0.16.0                  # HTTP/1.1 protocol library
httpcore==1.0.9              # HTTP client library
idna==3.11                   # Internationalized Domain Names
jiter==0.12.0                # JSON iterator library
sniffio==1.3.1               # Async library detection
typing-extensions==4.15.0    # Backported type hints
annotated-types==0.7.0       # Runtime type annotations
annotated-doc==0.0.4         # Annotated documentation
pydantic-core==2.41.5        # Core Pydantic functionality
typing-inspection==0.4.2     # Type inspection utilities
distro==1.9.0                # OS distribution information

# ============================================================================
# TESTING (Optional - Development)
# ============================================================================
pytest==7.4.3                # Testing framework
pytest-asyncio==0.21.1       # Async test support
pytest-cov==4.1.0            # Coverage plugin for pytest
# Note: httpx is already included in HTTP & API CLIENT section

# ============================================================================
# POST-INSTALLATION COMMANDS
# ============================================================================
# After installing requirements.txt, run the following commands:
#
# 1. Install Playwright browsers (REQUIRED):
#    playwright install
#    playwright install-deps  # Install system dependencies (Linux only)
#
# 2. For face-recognition, you may need to install dlib dependencies:
#    Windows: Download dlib wheel from https://github.com/z-mahmud22/Dlib_Windows_Python3.x/releases
#    Linux: sudo apt-get install build-essential cmake
#           sudo apt-get install libopenblas-dev liblapack-dev
#
# 3. FFmpeg system installation (if not using ffmpeg-python):
#    Windows: Download from https://ffmpeg.org/download.html and add to PATH
#    Linux: sudo apt-get install ffmpeg
#    macOS: brew install ffmpeg
#
# 4. For audio processing, you may need:
#    Windows: Install Visual C++ Build Tools
#    Linux: sudo apt-get install libsndfile1
#
# ============================================================================
